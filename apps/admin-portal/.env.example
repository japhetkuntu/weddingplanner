# Copy to .env for local development.

VITE_API_BASE_URL=http://localhost:5080
VITE_WEDDING_WEBSITE_URL=http://localhost:5175

# Netlify build: set the real values in the site's Environment variables UI
# (Site configuration -> Environment variables) — this file is just the schema,
# it is not read at build time.
